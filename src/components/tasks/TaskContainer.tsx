import React from "react";

interface TaskContainerProps {}

const TaskContainer: React.FC<TaskContainerProps> = (props) => {
  return <div>TaskContainer works!</div>;
};

export default TaskContainer;
